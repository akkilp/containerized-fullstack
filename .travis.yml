sudo: required
services:
  - docker

before_install:
  - docker build -t react-unit-test -f ./client/Dockerfile.dev ./client

script:
  - docker run react-unit-test npm test -- --coverage

after_success:
  - docker build -t akkilp/